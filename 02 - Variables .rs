// Variables in Rust
fn main() {
    let x = 10;      // Immutable variable
    let mut y = 20;  // Mutable variable
    y = 30;          // Changing value of y
    println!("x: {}, y: {}", x, y);
}
// Output: x: 10, y: 30